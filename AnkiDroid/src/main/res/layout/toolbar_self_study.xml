<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:background="?attr/toolbarBackgroundColor"
        android:minHeight="?attr/actionBarSize"
        android:theme="@style/ActionBarStyleNew"
        app:contentInsetStart="0dp"
        app:contentInsetStartWithNavigation="0dp"
        app:navigationContentDescription="@string/abc_action_bar_up_description"
        app:navigationIcon="?attr/navBarBackRef">
        <LinearLayout
            android:background="@drawable/deckpicker_search_background"
            android:gravity="center"
            android:layout_width="203dp"
            android:layout_height="36dp">
            <TextView
                android:id="@+id/toolbar_title"
                android:layout_width="wrap_content"
                android:gravity="center"
                android:layout_height="wrap_content"
                android:textColor="@color/new_primary_text_third_color"
                android:background="@drawable/deckpicker_search_background"
                android:textSize="15sp"
                app:drawableStartCompat="@mipmap/home_search_pressed" />
        </LinearLayout>


        <Spinner
            android:id="@+id/toolbar_spinner"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackground"
            android:visibility="gone"
            app:popupTheme="@style/ActionBar.Popup" />
    </androidx.appcompat.widget.Toolbar>
</merge>